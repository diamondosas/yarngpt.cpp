# YarnGPT Web UI

A simple web interface to test the local streaming server.

To use:
1. Start the Golang server.
2. Open `index.html` in your browser.
3. Enter text and listen to the streamed audio.
